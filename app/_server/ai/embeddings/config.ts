/**
 * Configurazione default per il modulo embeddings.
 */

export const GEMINI_EMBEDDING_MODEL = "gemini-embedding-001";

/** Dimensionalità di default per gli embedding (768, 1536 o 3072). */
export const DEFAULT_OUTPUT_DIMENSIONALITY = 768;
